import type { NextPage } from "next";
import { Recover_PasswordContainer } from "../container";

const Recover_Password: NextPage = () => <Recover_PasswordContainer />;

export default Recover_Password;
